// needed:
  // onload function
  //query select or getbyid all elements needed
  //event listener for submit button so we know its been clicked
  //if/else statement for calculation to be performed
  //variable for result

  //Business logic
  // calculation functions
function add(num1, num2) {
  return num1 + num2;
}

function sub(num1, num2) {
  return num1 - num2;
}

function mult(num1, num2) {
  return num1 * num2;
}

function div(num1, num2) {
  return num1 / num2;
}

  //UI logic
  //events and other things that make the functions run

